version: "3.8"

services:
  web:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - db

  db:
    image: mysql:5.7
    restart: always
    env_file: .env
    volumes:
      - dbdata:/var/lib/mysql

volumes:
  dbdata:
